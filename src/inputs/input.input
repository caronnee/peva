ROBOT robot1
{
	KILLED < 5
	VISIT_SEQ( START[robot2], START[robot1])
	VISIT( [100,200])

	main() // komentar
	{
		turnL();
		while(true)
		{
			shoot(0);
		}
	}
}
ROBOT robot2
{
	KILLED < 5
	VISIT_SEQ( START[robot1], START[robot2])
	VISIT( [100,200])

	main() // komentar
	{
		turnL();
	}
}
