TARGET		= sdl09a
CC		= g++
CFLAGS		= $(shell sdl-config --cflags)
LDFLAGS		= $(shell sdl-config --libs)

$(TARGET): sk.cpp
	$(CC) -o $(TARGET) sk.cpp $(CFLAGS) $(LDFLAGS) -lSDL_ttf

clean:
	rm -f $(TARGET) core
