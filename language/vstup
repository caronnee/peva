integer left, right; //povolene komentare, bool aj integer su naprosto stejne

integer function f(integer a)
{
	if ( a == 0 )
		return 1;
	return a+f(a-1);
}
main() // komentar
{
	integer a;
	a = f(4);
	turnL();
	a = 100;
}
